from setuptools import setup, find_packages

setup(
    name="diabetes_ml_project",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Dependencies are managed via requirements.txt,
        # but you can list core ones here if desired.
    ],
)